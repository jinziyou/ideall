{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "apps",
  "description": "本机已安装应用: 列举与启动 (仅桌面)",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "allow-list-installed-apps",
    "allow-launch-installed-app",
    "allow-read-app-icon-data-url"
  ]
}
