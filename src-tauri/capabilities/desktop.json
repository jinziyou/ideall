{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-updater",
  "description": "桌面专属能力：自动更新与 OAuth 环回回调监听（移动端不启用）。",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": ["updater:default", "allow-oauth-callback-start", "allow-oauth-callback-stop"]
}
