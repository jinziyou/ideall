# ideall 环境变量
# 复制为 .env.local 后按需修改（Next.js 自动加载 .env.local）
#
# ideall 仅以 App 形态运行（Tauri 静态导出，无 Node 生产服务端）。
# 客户端直连后端数据服务；地址在构建期内联进静态包，故主用 NEXT_PUBLIC_ 前缀（会进客户端 bundle）。
# home / tool 的本地能力不依赖后端；info / community 需要可用的 wonita 服务（需后端放行 CORS）。

# =============================================================================
# NEXT_PUBLIC_SERVER_ADDR — wonita 服务 (Rust Axum API) 基址（App 客户端直连）
# 仅 `pnpm app:export` / `pnpm app:build` 的静态导出会内联此地址。
# =============================================================================

# --- 模式 A：官方 Wonita 信息服务（普通用户）---
# 官方运营的采集 / 图谱 / 鉴权 API（域名 wonita.link，上线生效前可能暂不可达）。
# NEXT_PUBLIC_SERVER_ADDR=https://api.wonita.link

# --- 模式 B：本地开发 / 自托管后端 ---
# 在本机或内网自行运行 wonita 服务（及完整 wonita 后端栈）时使用。
NEXT_PUBLIC_SERVER_ADDR=http://127.0.0.1:5021

# =============================================================================
# SERVER_ADDR — 仅 `pnpm dev` 的 SSR 渲染期（供 Tauri 开发壳加载）在服务端读取。
# 不设时回退到 NEXT_PUBLIC_SERVER_ADDR / 本地默认。
# =============================================================================

# SERVER_ADDR=http://127.0.0.1:5021
