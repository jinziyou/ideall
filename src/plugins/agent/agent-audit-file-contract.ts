/** 只允许可信 UI / 内部 Agent runtime 通过 Agent audit FileSystem 写固定脱敏结构。 */
export {
  AGENT_AUDIT_APPEND_ACTION,
  AGENT_AUDIT_COMPLETE_ACTION,
  AGENT_AUDIT_WRITE_PERMISSION,
} from "@/filesystem/builtin-app-roots"
